// In an ideal world, i would create a monorepo to share types between frontend and backend
// but for the sake of this challenge, i will duplicate the necessary types here

export interface StatusField {
  label: string;
  sequence: number;
}
